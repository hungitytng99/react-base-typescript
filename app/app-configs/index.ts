export const Configs = {
  BASE_API: "https://dev.edtech.b4e.vn",
};

export enum REQUEST_STATE {
  ERROR = 'ERROR',
  REQUEST = 'REQUEST',
  SUCCESS = 'SUCCESS',
}

export enum USER_TYPE {
  ISSUER = 'Issuer',
  INSTITUTION = 'Institution',
}

export const TOKEN_KEY: string = 'token';
